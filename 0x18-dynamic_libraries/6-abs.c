#include "main.h"
/**
 * _abs - computes absolute value of an integer
 *
 * @l: number to be input
 *
 * Return: Absolute value of number or zero
 */
int _abs(int l)
{
if (l >= 0)
{
return (l);
}
else
{
return (l * -1);
}
}
