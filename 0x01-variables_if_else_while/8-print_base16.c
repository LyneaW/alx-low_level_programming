#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int l;
int w;

for (l = 48; l <= 57; l++)
{
putchar(l);
}
for (w = 97; w <= 102; w++)
{
putchar(w);
}
putchar('\n');
return (0);
}
